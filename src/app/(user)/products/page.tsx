import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Vodiy Parfum | Products",
    description: "Vodiy Parfum | Products - e-commerce website",
};

const ProductsPage = () => {
    return (
        <section>
            <h1>ProductsPage</h1>
        </section>
    );
};

export default ProductsPage;
